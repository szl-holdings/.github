# SZL Holdings — Estate Audit + Codex Upgrade Brief

**Generated:** 2026-09-20  
**Authority:** GitHub `szl-holdings` is canonical source. Hugging Face `SZLHOLDINGS` is publish / runtime mirror. Dual-write, never cut.  
**Decision:** `PRODUCTION_AUTHORIZED = HOLD`  
**Λ uniqueness:** Conjecture 1 — OPEN. Unconditional uniqueness is already false.  
**Locked Lean:** exactly `{F1, F4, F7, F11, F12, F18, F19, F22}`  
**Executable formula registry:** 21 names in `szl-formulas`  
**Ledger / wave cards ~180:** experimental or blank. Not theorems.  
**Trust ceiling:** 0.97. Doctrine v11 LOCKED (749 declarations / 14 unique axioms / 163 tracked sorries) — REPORTED from estate cards, not re-measured in this pass.  
**Product origin:** https://a-11-oy.com  
**Proof origin:** https://a11oy.net  
**Holdings pages:** https://holdings.a-11-oy.com (`szl-holdings.github.io`)  
**Never:** `a11oy.com` (wrong host)

This file is a Codex work order. It is not a merge, deploy, publication, or production certificate. A Space HTTP 200, download count, signature, or chat instruction is not owner approval.

---

## 0. What this thread already did (do not redo as a new flagship)

Session work produced **software operators**, not Lean proofs:

| Operator | Status | Where it belongs |
|---|---|---|
| Weighted geometric mean Λ | SOFTWARE / ADVISORY | `szl-formulas` + `szl-lambda-gate` |
| A2 homogeneity degree-1 `Λ(c·x)=c·Λ(x)` | SOFTWARE check; old `c^n` statement FALSE | same |
| ELECTRE III concordance / discordance / credibility S | SOFTWARE | sibling aggregator, not admissions plane |
| Choquet (additive + 2-additive) + Sugeno | SOFTWARE advisory | sibling aggregator |
| Fail-closed stack | evidence → ELECTRE veto → Λ zero-kill → Choquet score | a11oy formula endpoints / frontier console |
| Unsigned observation chain | UNSIGNED_HONEST | receipts UI |
| AgencyBench-V2 path in old payload | INVALID (`benchmark.jsonl` 404). Real path is `data/v2/v2.jsonl` | do not claim a run |

Do **not** mint `szl-frontier-ledger-app` as a public repo. Fold operators into existing keepers.

---

## 1. Live inventory (MEASURED 2026-09-20, with drift)

### GitHub `szl-holdings`

| Signal | Value | Label |
|---|---|---|
| Search API `org:szl-holdings` | 124 | MEASURED (includes private visible to this token) |
| Public org page | 117 | MEASURED |
| Archived (search sample) | 28 | MEASURED subset |
| Stars | ~0–1 | MEASURED |
| Default branch | `main` | REPORTED |
| License | Apache-2.0 almost everywhere; lake CC-BY-4.0 | REPORTED |

Private observed this pass: `szl-safe-stack`, `szl-estate-os`, `szl-org-health`. Treat private as out-of-scope for public PRs unless the owner names them.

### Hugging Face `SZLHOLDINGS`

| Signal | Value | Label |
|---|---|---|
| Models | 47 | MEASURED on org card this pass |
| Datasets | 35 | MEASURED on org card this pass |
| Spaces | 22 | MEASURED on org card this pass |
| Collections | 21 claimed on org / list pages vary | REPORTED (counts drift) |
| Followers | 3 | MEASURED |

`datasets/SZLHOLDINGS/SZLHOLDINGS` is a **historical profile mirror**. Do not infer current inventory from it.

### Origins (KEEP)

| Surface | Role |
|---|---|
| `a-11-oy.com` | Product / Command Center / console / product `/verify` |
| `a11oy.net` | Proof: RECORD, atlas, ROADMAP. Not runtime. |
| `holdings.a-11-oy.com` | Company front door (Pages) |
| `github.com/szl-holdings` | Canonical source |
| `huggingface.co/SZLHOLDINGS` | Artifact registry. Not a front door. |

---

## 2. KEEP fleet (do not archive, do not replace)

From `.github/docs/CANONICAL_FLEET.md` (2026-09-08):

**Public seven**

1. `szl-holdings/a11oy` — flagship Command Center  
2. `szl-holdings/killinchu` — public synthetic counter-UAS. No public effector.  
3. `szl-holdings/david-leads` — evidence-backed broker research. Not an A11oy hologram.  
4. `szl-holdings/anatomy` — living anatomy / organ map  
5. `szl-holdings/immune` — fail-closed kernel surface  
6. `szl-holdings/szl-real-estate` — occupancy UNAVAILABLE unless measured  
7. `szl-holdings/szl-atelier` — artifact showcase; source must stay active  

**Inference flagship**

8. `szl-holdings/szl-router` → Space `SZLHOLDINGS/llm-router-live`

**Proof / holdings / org**

9. `szl-holdings/a11oy-net`  
10. `szl-holdings/szl-holdings.github.io`  
11. `szl-holdings/.github` — reusable workflows + estate docs  

**Kernel / formula / receipt organs (upgrade in place)**

12. `szl-holdings/szl-formulas`  
13. `szl-holdings/szl-lambda-gate`  
14. `szl-holdings/szl-kernels`  
15. `szl-holdings/szl-receipt`  
16. `szl-holdings/lutar-lean`  
17. `szl-holdings/szl-frontier` — Hub admissions / HOLD-vs-PROMOTE (do not dump Choquet into this plane)  
18. `szl-holdings/frontier-bench`  
19. `szl-holdings/retrieval-bench`  
20. `szl-holdings/platform` — monorepo / MCP  
21. `szl-holdings/hatun-mcp`  

Wave 0 still DENYs minting new public repos, Spaces, or product names.

---

## 3. Do not revive (archived holograms / duplicates)

Map is already written on the archived repo descriptions. Examples:

| Archived | Canonical |
|---|---|
| `cosmos` | `anatomy` |
| `lean-kernel`, `szl-formula-ledger` | `lutar-lean` |
| `docs-site`, `developers` | `a11oy-net` |
| `khipu-pages` | `szl-khipu` |
| `lambda-gate-holo`, `governed-norm-holo` | `szl-lambda-gate` |
| `energy-attest-holo` | `szl-energy-attest` |
| `receipt-chain-live` | `szl-receipt` |
| `szl-kernels-live` | `szl-kernels` |
| `ouroboros` | `platform` + `szl-ouroboros` |
| `szl-governed-norm` | DEPRECATED → `szl-lambda-gate` |

Warhacker is a historical tag / evidence snapshot. Do not make it a Space. Do not pin it as a product.

---

## 4. Hugging Face surfaces to upgrade (adapters, not one copied shell)

**Pinned / keeper Spaces (treat as live product UI):**  
`a11oy`, `killinchu`, `david-leads`, `SZL-KHIPU` / khipu, `szl-atelier`, `llm-router-live`.

**Other running Spaces seen this pass (partial):**  
Living Anatomy, SZL Cosmos (hologram of anatomy — do not promote as second flagship), command lab / Atlas, constellation, the-grid, receipt-chain live, szl-blocked-live, energy / governed-norm holos, model labs.

**Kernel models (software ground truth + optional surrogate):**  
`szl-formulas` (21 formulas; joblib QUARANTINED), `szl-lambda-gate`, `szl-invariants` (8 falsifiable invariants), `szl-blocked`, `szl-govsign`, `szl-provctl`, `szl-kernels`, `szl-ouroboros`.

**Weight lineage — CANDIDATE only, cards already say so:**  
`SZL-Khipu-1.5B` (+ GGUF), `SZL-Forge-1.5B-ReceiptAgent`, qwen35-0.8b receiptagent family, chaski / willay / brain-navigator, ReceiptAgent-Nano, TinyKhipu-Nano. Do not write “production model.”

**Datasets:**  
`szl-lake`, `killinchu-osint-corpus`, `a11oy-verifiable-corpus`, `canonical-formulas-v1`, thesis / UDS receipt sets, `szl-frontier-evaluation-receipts`, `szl-frontier-covenant`. Membership ≠ quality ranking.

---

## 5. Frontend upgrade contract (already exists — implement it)

Source: `.github/docs/HF_UNIVERSAL_FRONTEND_ACTION_V1.md`

- One **shared token layer**, many **framework-native adapters**. Do not copy one Gradio shell onto React / Static / Docker / Streamlit.
- Tokens: KANCHAY palette already used on holdings pages (ink / parchment / copper). Do not invent a second brand.
- Viewport classes, **44px** touch targets, **no horizontal overflow**, `prefers-reduced-motion`, wrap technical identifiers.
- Honest chips only: `MEASURED` / `REPORTED` / `MODELED` / `HEURISTIC` / `UNKNOWN` / `UNAVAILABLE` / `HOLD` / `BLOCKED` / `INVALID`.
- Card metadata boundaries stay on the Hub card. Pages HTML is not runtime health.
- Reusable frontend verifier is **read-only**, SHA-pinned, no tokens, no writes.
- Flagship FE blob is `a11oy` (including `a11oy_code_ide.html` and product CSS), then `a11oy-net`, then `szl-holdings.github.io`, then each Space adapter.

### Per-surface FE work

| Surface | Framework (REPORTED) | Work |
|---|---|---|
| `a11oy` | Python + large HTML/CSS | Shared tokens, HOLD chips, formula panel shows 21 + locked-8 + Conjecture 1 OPEN, ELECTRE/Choquet as separate operators |
| `a11oy-net` | Static HTML | Proof language only. No “live runtime” claims |
| `szl-holdings.github.io` | Static | Company door. Pointers to console/verify, not clones |
| `killinchu` | Space | Synthetic disclaimer visible. No effector UI |
| `david-leads` | Space | Evidence receipts; not A11oy chrome clone |
| `llm-router-live` | Space | Status + per-answer receipt. No credentials |
| `szl-atelier` / Atlas / constellation | Space | Inventory explorer. Counts labeled MEASURED-at-time |
| `immune` / `anatomy` | Space | Keep organ language. No second flagship |

---

## 6. Backend upgrade contract

### 6.1 Formula / governance kernel

Implement in `szl-formulas` (canonical 21) and expose via a11oy formula endpoints:

1. Keep `lambda_aggregate` as WGM. Label uniqueness CONJECTURE.  
2. Keep A2 as degree-1. Add a regression test that `c^n` fails.  
3. Add `electre_iii_credibility`, `choquet_discrete`, `choquet_2additive`, `sugeno` as **new names**, not replacements for Λ.  
4. Gate order: policy/evidence → ELECTRE veto → Λ zero-kill → Choquet advisory.  
5. Do not change locked-8 IDs. Do not map F-numbers onto the 21 if mapping is UNKNOWN.  
6. Do not load Hub `model.joblib` in any default path (quarantine).

### 6.2 Receipts

- `szl-receipt` is the one signing primitive.  
- UI and APIs must distinguish `UNSIGNED_HONEST` vs DSSE/ECDSA verified.  
- Never coerce a missing signature into “signed.”  
- Chain continuity is an invariant (`szl-invariants`), not a production certificate.

### 6.3 Admissions vs operators

- `szl-frontier` engine = catalog / Hub evidence / HOLD-vs-PROMOTE.  
- Aggregators belong in `szl-formulas` or `a11oy` formula module.  
- A green software check is not `QUALIFIED` and not `PRODUCTION_AUTHORIZED`.

### 6.4 Benches

- `frontier-bench`: unconfigured engines emit `BLOCKED`, never invented TTFT.  
- `retrieval-bench`: five-state honesty contract.  
- Wire public scoreboards to those states. No “#1” copy.

### 6.5 Router

- Bound fallback. Per-answer receipt.  
- Do not print provider keys, private hosts, or topology.  
- Provider-backed inference needs separately admitted config.

---

## 7. Work orders (Codex executes as draft PRs only)

| ID | Repo | Title | Done when |
|---|---|---|---|
| E1 | `a11oy` | Shared tokens + honest chips + operator panel | FE passes Universal Frontend checks; Λ labeled CONJECTURE |
| E2 | `a11oy-net` | Proof-site token + language audit | No runtime-health claims |
| E3 | `szl-holdings.github.io` | Holdings door token sync | Pointers only |
| E4 | `szl-formulas` | ELECTRE + Choquet + Sugeno + A2 tests | 21 registry intact; new names; joblib still quarantined |
| E5 | `szl-lambda-gate` | Document stack order; do not claim uniqueness | Card + kernel comments |
| E6 | `szl-frontier` | Sibling aggregator import only if needed | Admissions plane unchanged |
| E7 | `killinchu` + `david-leads` + `immune` + `anatomy` | Adapter FE, 44px, overflow, chips | No shell-copy |
| E8 | `szl-router` + `llm-router-live` | Receipt visibility + BLOCKED states | No secret leakage |
| E9 | `frontier-bench` + `retrieval-bench` | Public scoreboard contract | BLOCKED ≠ 0.000 fake |
| E10 | `szl-receipt` + a11oy receipt UI | UNSIGNED vs signed | Self-check still honest |
| E11 | `.github` | Pin frontend verifier SHA in callers | No `@main` float |
| E12 | alignment JSON | Regenerate GH↔HF inventory from live APIs | Drift listed, not invented |

Each PR: one repo, one concern, `HOLD` in the description, no deploy step.

---

## 8. Forbidden actions (hard fail)

- `bypass_authentication`, `extract_secrets`  
- `scrape_private_data`, `scrape_paywalled_content`  
- `download_gated_weights_without_permission`  
- `rehost_model_artifacts_without_distribution_rights`  
- `fabricate_hardware_receipts`, `fabricate_benchmark_results`, `fabricate_independent_review`  
- `weaken_required_checks`, `bypass_branch_protection`  
- `merge_pr_marked_do_not_merge`  
- `deploy_without_explicit_approval`, `publish_without_explicit_approval`  
- `claim_production_without_measured_evidence`  
- `claim_number_one_without_comparative_benchmark_evidence`  
- Mint a new public repo / Space / product name  
- Revive archived holograms  
- Close Conjecture 1  
- Claim 180 locked theorems  
- Load quarantined joblib by default  
- Archive a KEEP fleet repo  

---

## 9. Promotion gate (mechanical)

Required for `PRODUCTION_AUTHORIZED` and **all missing today**:

`qualified_receipt`, `target_environment`, `deployment_receipt`, `monitoring_receipt`, `rollback_plan`, `explicit_release_approval`, `post_deploy_verification`

Also missing for any “we shipped production”:

- Isolated eval with pinned artifact + digest  
- Dataset identity + license  
- Hardware topology + energy method (or honest `UNAVAILABLE`)  
- Independent review  
- Named owner signature  

Until those exist: **HOLD**.

---

## 10. How Codex should start

```text
1. Read this file and run szl_codex_upgrade_payload.py --emit
2. Open ONLY the repo named in the work order
3. Branch from main; do not push to main
4. Implement the smallest diff that satisfies the "Done when" column
5. Add tests that fail closed (BLOCKED / HOLD / UNKNOWN)
6. Open a DRAFT PR. Body must include: Conjecture 1 OPEN, locked-8 frozen, PRODUCTION HOLD
7. Stop. Wait for owner review.
```

Frontier work that is actually new (and allowed): ELECTRE veto that fires when Λ would still ALLOW; 2-additive Choquet as an interaction score; honest BLOCKED benches; one token system across keepers. That is the push. Invention of a second flagship is not.
