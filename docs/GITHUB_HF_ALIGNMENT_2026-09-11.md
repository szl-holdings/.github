# GitHub / Hugging Face / product / proof alignment

**Status:** `SOURCE_MIRROR_PRODUCT_PROOF_RECAPTURED`
**Captured:** 2026-09-11T22:19:13Z
**Evidence class:** MEASURED for public HTTP and anonymous Hub API; REPORTED for authenticated GitHub search
**Runtime:** DEMO / pre-launch. HTTP 200 is reachability, not ATO.

GitHub `szl-holdings` is protected source.
Hugging Face `SZLHOLDINGS` is the artifact / runtime mirror.
`https://a-11-oy.com` is the product origin.
`https://a11oy.net` is the proof origin.
Never `a11oy.com`.

## Live recapture

- Product apex HTTP 200, `server=szl`, `x-szl-space: a11oy`, `x-szl-wire-d: LIVE`.
- Honesty `git_sha=0abde0c4114bdc8c603b984e884f657d74f9ab88`. Doctrine v11 LOCKED 749/14/163 @ `c7c0ba17`. Lambda = Conjecture 1.
- Health rollup signer `DSSE-LIVE`. Fingerprint `9926bf69b799ea663fc5cf5a8c5d8f594d99d7b323700d6a20b803bd4c9f4e15`.
- Signing-status REAL ECDSA-P256. `/cosign.pub`, `/console`, `/verify` HTTP 200.
- `/spectral` and `/controller` HTTP 404. `www.a-11-oy.com` TLS UNAVAILABLE.
- Proof apex, `/record/`, `/estate/` HTTP 200.
- Anonymous Hub: 46 models, 35 datasets, 21 Spaces. MATCH vs a11oy#2010 public predicate.
- Do not merge with Series-A counts (spaces 26, datasets 43).
- GitHub org search: 123 public hits. Flagship `a11oy` admin-reachable.

## Python / Hub rules

1. GitHub is kernel source. Hub is publish mirror.
2. `model.joblib` stays QUARANTINED. Load via `kernels.get_kernel` after reading the GitHub twin.
3. Weights bind to `szl-forge`. GGUF binds to `szl-serve`.
4. `SZL_HF_FRONTIER_WIRE.py` seed 43 is STALE versus MEASURED 46. Do not silent-bump without fixtures.
5. Use `/api/a11oy/healthz` and `/api/a11oy/khipu/sign`. Do not use `/api/a11oy/v1/ledger` as receipt UI.
6. Stale verify route still prints `DSSE_PLACEHOLDER`. Verify against `/cosign.pub`.

## Not proved

Not ATO, GPU inference, www TLS, energy-ledger chain (a11oy#1878), Hub 1.31 consumption (a11oy#2087), or counsel/finance/lyte/terra source-revision match (a11oy#2010).

Owner queue remains: Cloudflare token, HF write token, Nemo v3 ceremony, org code scanning.
