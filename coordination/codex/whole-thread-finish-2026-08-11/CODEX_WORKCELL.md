<!-- SZL-CODEX-WHOLE-THREAD-FINISH-V1 -->
# Codex Master Workcell — Finish the Whole SZL Thread

**Owner authorization:** Stephen P. Lutar Jr. / SZL Holdings  
**Coordination repository:** `szl-holdings/.github`  
**Execution date:** 2026-08-11  
**Primary organizations:** GitHub `szl-holdings`; Hugging Face `SZLHOLDINGS`  
**Primary domains:** `a-11-oy.com`; `a11oy.net`  
**Operating standard:** Zero-Bandaid Law; evidence before claims; protected delivery only.

## Mission

Codex must treat this workcell as the canonical continuation of the long-running SZL build thread. Finish the estate as one governed, proof-carrying operating system rather than a collection of partially aligned repositories, Spaces, models, datasets, kernels, collections, buckets, front ends, and deployment scripts.

Do not stop at reconnaissance, a roadmap, a mock, an issue-only handoff, or a local patch. Execute the complete cycle wherever authenticated transports and managed credentials exist:

`recapture → plan → branch → implement → test → PR → protected merge → deploy → exact-revision verification → signed receipt → issue closure`

A truthful terminal failure is acceptable. A fabricated green state is not.

## Authority and boundaries

The owner authorizes reversible and governed execution across the SZL estate. This authorization does **not** permit weakening repository protections or bypassing evidence requirements.

- Never push directly to a protected default branch.
- Never force-push, rewrite shared history, disable rulesets, dismiss legitimate reviews, or lower required checks.
- Never print, copy, commit, or transmit secret values. Use managed GitHub, Hugging Face, database, cloud, and signing credentials only through their intended secret stores.
- Never search browser stores, shell history, arbitrary workstation files, or unrelated repositories for credentials.
- Never claim production, customer, compliance, certification, ATO, revenue, benchmark, deployment, or proof status without a current machine-readable receipt.
- Never copy leaked prompts, proprietary model instructions, private datasets, unlicensed weights, or vendor trade dress.
- Treat `Lambda = Conjecture 1` as advisory and never as a theorem or universal green gate.
- Preserve the estate’s evidence vocabulary: `MEASURED`, `REPORTED`, `MODELED`, `HEURISTIC`, `UNKNOWN`, `UNAVAILABLE`, `CONJECTURE`, `TERMINAL_FAILURE`, `VERIFIED_CURRENT`.

## Historical findings to recapture, not blindly trust

The July estate audit reported two shipping flagships (`a11oy`, `killinchu`), three additional live Docker organs (`yarqa`, `anatomy`, `hatun-mcp`), a 26-Space Hugging Face roster, stale model/dataset counts, an unresolved Killinchu deployment contradiction, indefinite `CHECKING`/`LOADING` states on both public domains, a stale Sentra reference, and two competing MCP authorities. Those observations are reconnaissance seeds only. Recapture every mutable count, branch head, issue state, deployment revision, Hub inventory, and runtime result before mutation.

## Phase 0 — Immutable current-state capture

Before editing any repository:

1. Read the repository root `AGENTS.md`, contribution rules, security policy, release doctrine, known-gaps register, and local artifact instructions.
2. Capture for every in-scope repository:
   - default branch and full 40-character head OID;
   - branch protections and rulesets;
   - open pull requests and issues;
   - required checks and current workflow health;
   - release/deployment workflow;
   - active domains, services, Spaces, models, datasets, kernels, collections, and buckets;
   - current deployed source revision or immutable artifact digest;
   - current public claim surface.
3. Capture Hugging Face inventory with full pagination and immutable revision IDs. Record public and authorized-private assets separately. Never convert a rate limit, API error, or partial page into a zero count.
4. Capture database identity and migration state without logging connection material.
5. Write:
   - `coordination/codex/whole-thread-finish-2026-08-11/current-state.json`
   - `coordination/codex/whole-thread-finish-2026-08-11/workgraph.json`
   - `coordination/codex/whole-thread-finish-2026-08-11/decision-log.md`
6. Diff the current capture against historical claims. Current evidence wins.

## Workstream A — GitHub organization convergence

**Repositories:** `.github`, `a11oy`, `platform`, `killinchu`, `hatun-mcp`, `szl-forge`, `lutar-lean`, and every currently active supporting repository discovered in Phase 0.

1. Reconcile every open PR and issue against current default-branch state.
2. Close duplicate, superseded, or already-satisfied issues only with exact evidence.
3. Repair actionable red workflows at the root; classify unavoidable infrastructure failures explicitly rather than hiding them.
4. Preserve full-SHA pinning for third-party Actions, CodeQL, dependency review, secret scanning, workflow linting, DCO, signed-commit policy, conversation resolution, exact-head checks, and linear history.
5. Establish one generated organization inventory distinguishing:
   - shipping flagships;
   - live supporting organs;
   - libraries and infrastructure;
   - archived or retired repositories;
   - experimental research.
6. Remove stale counts and unsupported marketing language from org profiles and repository READMEs.
7. Do not manufacture an independent reviewer. If no eligible independent collaborator exists, preserve all mechanical gates and record the governance limitation honestly.

## Workstream B — A11oy and public-domain operational truth

**Repositories:** `a11oy`, `a11oy-net`, `killinchu`.

### B1. Terminal health states

For `a-11-oy.com`, `/status`, `/spaces`, `/gap-report`, `/verify`, `/assurance`, `/energy`, and `a11oy.net`:

- launch independent probes concurrently;
- enforce a hard wall-clock deadline around both network fetch and body parsing;
- settle every card to `REACHABLE`, `DEGRADED`, `UNREACHABLE`, or `UNAVAILABLE`;
- prevent one hung endpoint from delaying unrelated cards;
- provide a server-side or static terminal fallback for crawlers and no-JavaScript clients;
- retain cached evidence only with source, timestamp, digest, and `CACHED` label;
- eliminate permanent `CHECKING`, `PROBING`, `LOADING`, blank-table, and spinner-only states;
- sanitize all text, URLs, and routes;
- add tests for never-settling fetches, parsers, malformed JSON, hostile URLs, and partial API responses.

### B2. Killinchu capability-axis truth

Replace the binary “deployed / not deployed” contradiction with independently evidenced axes:

- runtime reachability;
- exact source binding;
- decoder implementation;
- sensor/input availability;
- simulation boundaries;
- effectors;
- customer readiness;
- security posture;
- compliance/certification;
- production authorization.

HTTP reachability must never imply customer, compliance, certification, or authorization readiness. Conversely, a deployed demonstration must not be described as “spec only / not deployed” when current build evidence proves a live runtime.

### B3. Exact-revision deployment proof

Bind one immutable source OID through build, artifact/image digest, publication revision, deployment revision, runtime build-info, browser verification, and final receipt. Reject any mismatch. Do not repair mismatches by editing an expected SHA after deployment.

## Workstream C — Hugging Face organization, models, kernels, datasets, collections, and buckets

**Organization:** `SZLHOLDINGS`  
**Source authority:** GitHub manifests and protected source revisions.

1. Generate canonical manifests for:
   - models;
   - governed kernels;
   - datasets/Lake;
   - Spaces;
   - collections;
   - public buckets.
2. Every record must contain:
   - canonical repository ID;
   - immutable GitHub and Hub revisions;
   - publisher and provenance;
   - license and redistribution rights;
   - expected files and SHA-256 digests;
   - model or dataset lineage;
   - intended role;
   - deployment target;
   - measured or explicitly unknown status.
3. Fix empty-inventory finalizers as explicit states. Never insert dummy objects to satisfy `min()`, `max()`, or count assumptions.
4. Remove static source locks from reconciliation workflows. Capture exact current sources at run start and bind them throughout the run.
5. Standardize every public org card, model card, dataset card, Space card, collection, and README for desktop and mobile:
   - concise investor summary;
   - developer quick start;
   - current evidence status;
   - license/provenance;
   - limitations;
   - links to source and verification;
   - no stale retired brands or unsupported claims.
6. Preserve GitHub as source authority; Hugging Face is a revision-locked publication target, not a second mutable source of truth.

## Workstream D — Governed model frontier and clean-room behavior synthesis

The public models previously discussed—including Qwen/QwyThos/Mythos/Fable/Heretic/“uncensored” variants—are discovery inputs only. Names, download counts, and social popularity are not evidence of origin, quality, safety, or license.

### D1. Model Admission Passport

Create a deny-by-default admission schema containing:

- canonical Hub ID and immutable revision;
- publisher;
- architecture and base lineage;
- exact weight and tokenizer digests;
- parameter count and context-claim source;
- format and quantization method;
- license and redistribution rights;
- training-data disclosure;
- prompt/template provenance;
- remote-code requirement;
- unsafe or ambiguous naming indicators;
- intended and prohibited uses;
- hardware fit;
- supply-chain inspection;
- measured evaluation vector;
- promotion state;
- receipt chain.

Unknown license, unknown provenance, unexpected executable code, missing files, or digest drift must quarantine the candidate.

### D2. Clean-room behavior profiles

Use only original SZL-authored profiles to transfer disposition—not weight-bound capability. Create governed profiles for:

- high-autonomy operator;
- adversarial verifier;
- evidence-first researcher;
- bounded coding agent;
- database change controller;
- incident commander.

Profiles may change autonomy posture, grounding habits, tool discipline, verification behavior, and voice. They must never claim to increase the underlying model’s reasoning ceiling. Store body digests, sources, licenses, and receipts. Never include leaked or verbatim third-party system prompts.

### D3. Pareto evaluation

Evaluate candidates and champions on separate axes:

- calibration/honesty;
- instruction compliance;
- refusal precision;
- tool-use safety;
- code correctness;
- retrieval grounding;
- long-context retention;
- adversarial robustness;
- identity/source attribution;
- license-boundary reasoning;
- execution-boundary reasoning;
- latency;
- memory;
- energy where measured;
- cost where measured.

Publish raw cases, harness version, preregistration, seeds, hardware identity, and per-axis results. Do not train on holdouts. Do not collapse the decision into one vanity score.

### D4. Training and promotion

For every new SZL model generation:

- preserve failed candidates and failure receipts;
- create a new candidate ID rather than relabeling failed weights;
- freeze holdouts before training;
- require every mandatory holdout to pass;
- prohibit automatic retry and threshold weakening;
- separate qualification from promotion;
- require signed train, merge, quantize, evaluate, publish, and deploy receipts.

## Workstream E — `szl-forge` productionization

1. Reconcile current Forge, Khipu, ReceiptAgent, Nemo, GGUF, Ollama, Unsloth, evaluation, signing, and publication paths.
2. Replace laptop-specific assumptions with explicit hardware profiles and bounded resource checks.
3. Preserve honest failure isolation between training, merge, conversion, quantization, import, inference, and evaluation.
4. Make every command rerun-safe and content-addressed.
5. Bind model cards and published artifacts to exact source, dataset, config, toolchain, and weight digests.
6. Integrate admitted models with the A11oy registry and routing layer without hard-coded fabricated availability.

## Workstream F — Series A database upgrade

**Repository:** `platform`.

1. Recapture the current database provider, PostgreSQL version, schema, trackers, migration journal, manual migrations, and application source revision.
2. Make journal-based migration the default. Restrict `drizzle-kit push` to explicitly acknowledged local development and block `push --force` from production and CI.
3. Validate:
   - every journal entry has a file;
   - hashes match;
   - tracker history satisfies the repository contract;
   - manual migration checksums match;
   - no unknown tracker entries exist;
   - fresh targets are genuinely empty;
   - legacy tracker backfill occurs only after full schema equivalence, never from a single sentinel table.
4. Production preparation requires:
   - exact protected-main OID;
   - immutable migration manifest;
   - encrypted backup;
   - successful restore to a distinct empty target;
   - schema, sequence, constraint, index, extension, and row-count comparison;
   - approved change ticket;
   - advisory lock;
   - write-fence evidence for destructive or rewrite-risk plans.
5. Apply through the managed database secret. Do not discover or print connection material.
6. Verify API, workers, telemetry, migration trackers, schema fingerprint, and representative governed workflows.
7. Emit a signed before/after cutover receipt. Do not call the DBA work complete without live database evidence.

## Workstream G — MCP authority unification

**Repositories:** `hatun-mcp`, `platform`, `a11oy`.

1. Inventory standalone Hatun MCP and the monorepo MCP gateway.
2. Select one canonical source contract based on doctrine, active consumers, deployment ownership, tests, and release path.
3. Generate compatibility layers from the canonical schema.
4. Remove duplicated hand-maintained tool definitions.
5. Add cross-repository drift tests for method names, input/output schemas, policy gates, receipts, and errors.
6. Publish compatibility and migration notices before retiring any entry point.
7. Do not create a third MCP registry.

## Workstream H — Frontend, mobile, and investor/developer presentation

Across GitHub, Hugging Face, domains, and deployable applications:

- enforce responsive behavior on common mobile, tablet, laptop, and widescreen breakpoints;
- eliminate clipping, overflow, illegible tables, tiny touch targets, dead links, placeholder states, and inconsistent navigation;
- use original SZL visual language rather than copied vendor trade dress;
- present one clear product hierarchy: flagships, organs, research, infrastructure, retired history;
- provide investor-facing value, market, moat, governance, evidence, and risk explanations;
- provide developer-facing architecture, quick start, API, model, data, deployment, test, and verification instructions;
- capture live screenshots for every modified public UI surface;
- run accessibility, route, link, browser, performance, and mobile viewport gates.

## Workstream I — Deployments and exact live verification

For each shipping surface:

1. Deploy only after protected merge.
2. Bind the deployment to the merged full OID and immutable artifact digest.
3. Verify live build-info, health, critical routes, browser state, model registry, receipts, and public claims independently of the deployment workflow.
4. Require rollback or forward-repair instructions and evidence.
5. Attach deployment receipts to the corresponding PR and issue.

## Repository mutation protocol

For every repository mutation:

1. Fetch protected default branch.
2. Capture full base OID.
3. Establish clean baseline and record relevant tests.
4. Create one non-protected branch for one coherent concern.
5. Implement the smallest root-cause change.
6. Run native tests, security gates, source-contract checks, and secret scans.
7. Create a DCO-certified commit using the repository’s signing policy.
8. Push normally.
9. Open a PR containing root cause, scope, tests, evidence, deployment boundary, and repair plan.
10. Observe required checks to terminal state.
11. Resolve review conversations.
12. Revalidate exact head before merge.
13. Use the protected merge path.
14. Deploy the merged exact revision.
15. Perform independent live readback.
16. Attach the receipt and close the governing issue only after acceptance passes.

## Required final artifacts

Create and maintain under this coordination directory:

- `current-state.json`
- `workgraph.json`
- `decision-log.md`
- `risk-register.md`
- `final-state.json`
- `receipts/`
- `claim-drift-report.md`
- `model-admission-report.json`
- `hf-estate-manifest.json`
- `github-estate-manifest.json`
- `database-cutover-receipt.json` or an honest managed-prerequisite block receipt
- `deployment-reconciliation.json`
- `FINAL_SERIES_A_REPORT.md`

## Definition of done

This whole-thread workcell is complete only when:

- every in-scope repository and Hub asset is represented in a current manifest;
- required PRs are merged through protected paths or explicitly terminally failed;
- required checks are terminal and explained;
- no public UI remains indefinitely `CHECKING`, `LOADING`, or blank;
- Killinchu’s public status is capability-axis accurate;
- source, artifact, publication, and deployed revisions agree for each shipping surface;
- every promoted model, dataset, and kernel has rights, provenance, digests, evaluations, and receipts;
- the database is live-verified or explicitly blocked by a named managed prerequisite;
- one MCP authority and generated compatibility contract exist;
- responsive desktop/mobile presentation is verified;
- stale counts, retired names, and unsupported claims are removed;
- the final receipt verifies offline;
- residual risks are explicit;
- no statement says “complete,” “operational,” “production,” “Series A ready,” or “verified” without matching evidence.

## Mandatory Codex final response

Return one row per workstream with:

- repository or organization;
- base OID;
- branch and PR;
- merged OID;
- deployed OID or artifact digest;
- tests/checks;
- receipt path;
- terminal status;
- residual risk.

Allowed terminal statuses are:

- `VERIFIED_CURRENT`
- `TERMINAL_FAILURE`
- `BLOCKED_MANAGED_PREREQUISITE`

Do not end with a plan. End with the evidence-backed execution matrix.