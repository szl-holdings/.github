<!-- SZL-CODEX-WHOLE-THREAD-FINISH-V1 -->
# Codex Master Workcell — Finish the Whole SZL Thread

**Owner authorization:** Stephen P. Lutar Jr. / SZL Holdings<br>
**GitHub organization:** `szl-holdings`<br>
**Hugging Face organization:** `SZLHOLDINGS`<br>
**Primary domains:** `a-11-oy.com`, `a11oy.net`<br>
**Execution standard:** current-state recapture → protected branch → implementation → tests → PR → protected merge → deployment → exact-revision readback → signed receipt.

## Mission

Continue and finish the long-running SZL build thread as one governed, proof-carrying operating system. The scope includes GitHub repositories and governance, Hugging Face models/kernels/datasets/Spaces/collections/buckets, A11oy and Killinchu public truth, terminal health UX, SZL Forge and model admission, the platform database cutover, MCP authority unification, responsive investor/developer presentation, deployments, and final evidence reconciliation.

Do not end at a roadmap, issue handoff, mock, local-only patch, or review. Execute every available workstream through repository-native delivery. A truthful terminal failure or named managed-prerequisite block is acceptable; fabricated success is not.

## Non-negotiable law

- Never push directly to a protected default branch.
- Never force-push, rewrite shared history, disable rulesets, dismiss legitimate reviews, or lower required checks.
- Never print, commit, copy, or transmit secret values. Use only managed GitHub, Hugging Face, database, cloud, and signing secret stores.
- Never search browser stores, shell history, arbitrary workstations, or unrelated repositories for credentials.
- Never claim production, customer, certification, compliance, ATO, revenue, deployment, proof, or benchmark status without current machine-readable evidence.
- Never copy leaked prompts, proprietary model instructions, private datasets, unlicensed weights, or vendor trade dress.
- Treat `Lambda = Conjecture 1` as advisory, never a theorem or universal green gate.
- Preserve the evidence vocabulary: `MEASURED`, `REPORTED`, `MODELED`, `HEURISTIC`, `UNKNOWN`, `UNAVAILABLE`, `CONJECTURE`, `TERMINAL_FAILURE`, `VERIFIED_CURRENT`.
- Historical audits and counts are reconnaissance seeds only; recapture every mutable fact before acting.

## Phase 0 — Immutable current-state recapture

Before editing any repository:

1. Read root and scoped `AGENTS.md` files, contribution rules, security policy, release doctrine, known-gaps register, and artifact instructions.
2. Capture for every active repository:
   - default branch and full 40-character head OID;
   - protections/rulesets, required checks, review policy, and signing/DCO requirements;
   - open PRs/issues and their exact heads;
   - release and deployment workflows;
   - current deployed revision or immutable artifact digest;
   - current public claims and evidence sources.
3. Capture the complete Hugging Face inventory with pagination and immutable revisions. Record API errors, rate limits, and partial pages explicitly; never turn them into zero counts.
4. Capture database provider/version, target identity, schema fingerprint, migration trackers, and application source without logging connection material.
5. Create under this directory:
   - `current-state.json`
   - `workgraph.json`
   - `decision-log.md`
   - `risk-register.md`
6. Diff current evidence against historical claims. Current evidence wins.

## W1 — GitHub organization convergence

**Scope:** `.github`, `a11oy`, `platform`, `killinchu`, `hatun-mcp`, `szl-forge`, `lutar-lean`, and all active supporting repositories found in Phase 0.

- Reconcile every open PR and issue against current default-branch state.
- Repair actionable red workflows at the root; classify infrastructure and intentional failures rather than hiding them.
- Preserve full-SHA Action pinning, CodeQL, dependency review, secret scanning, workflow linting, DCO, signing policy, conversation resolution, exact-head checks, and protected merge paths.
- Generate one current organization manifest separating shipping flagships, live organs, libraries/infrastructure, research, archived history, and retired names.
- Remove stale counts and unsupported marketing copy.
- Do not manufacture independent approval when no eligible reviewer exists; record the limitation while preserving mechanical gates.

## W2 — A11oy, Killinchu, and public-domain truth

**Scope:** `a11oy`, `a11oy-net`, `killinchu`, `a-11-oy.com`, `a11oy.net`.

### Terminal health

- Start independent probes concurrently.
- Enforce a hard wall-clock deadline around fetch and body parsing.
- Settle every card to `REACHABLE`, `DEGRADED`, `UNREACHABLE`, or `UNAVAILABLE`.
- Prevent one hanging endpoint from delaying unrelated cards.
- Provide server-side/static terminal fallbacks for crawlers and no-JavaScript clients.
- Label cached evidence with source, timestamp, digest, and `CACHED`.
- Eliminate indefinite `CHECKING`, `PROBING`, `LOADING`, blank-table, and spinner-only states.
- Sanitize rendered text, URLs, and routes.
- Test never-settling fetches/parsers, malformed JSON, hostile URLs, and partial API responses.

### Killinchu capability axes

Replace binary deployed/not-deployed copy with separately evidenced axes:

- runtime reachability;
- exact source binding;
- decoder implementation;
- sensor/input availability;
- simulation boundaries;
- effectors;
- customer readiness;
- security posture;
- compliance/certification;
- production authorization.

HTTP reachability must not imply customer, certification, compliance, or authorization readiness. A deployed demonstration must not be described as “spec only / not deployed” when exact build evidence proves a live runtime.

### Deployment proof

Bind one immutable source OID through build, artifact/image digest, Hub publication revision, deployed revision, runtime build-info, browser verification, and final receipt. Reject any mismatch; never repair one by changing an expected SHA after deployment.

## W3 — Hugging Face estate convergence

**Scope:** every authorized `SZLHOLDINGS` model, governed kernel, dataset/Lake, Space, collection, and bucket.

- Generate canonical manifests containing repository ID, immutable GitHub/Hub revisions, publisher/provenance, license/rights, expected files/digests, lineage, intended role, target, and measured-or-unknown status.
- Fix empty-inventory finalizers as explicit states; never insert dummy objects to satisfy `min()`, `max()`, or count assumptions.
- Remove static source locks from reconciliation jobs; capture exact source revisions at run start and bind them through publication and verification.
- Standardize public cards and READMEs for mobile and desktop with an investor summary, developer quick start, evidence status, license/provenance, limitations, source, and verification links.
- Remove stale retired brands and unsupported claims.
- Keep GitHub as source authority; Hugging Face is a revision-locked publication target.

## W4 — Governed model frontier

Public Qwen/QwyThos/Mythos/Fable/Heretic/“uncensored” candidates are discovery inputs only. Names, downloads, and popularity do not establish origin, quality, safety, or license.

### Admission passport

Deny by default unless a candidate records:

- canonical Hub ID and immutable revision;
- publisher, architecture, and base lineage;
- exact weight/tokenizer digests;
- parameter/context claim sources;
- format and quantization;
- license and redistribution rights;
- training-data and prompt/template provenance;
- remote-code requirement;
- ambiguous or unsafe naming indicators;
- intended/prohibited uses;
- hardware fit;
- supply-chain inspection;
- measured evaluation vector;
- promotion state and receipt chain.

Unknown license/provenance, unexpected executable code, missing files, or hash drift requires quarantine.

### Clean-room behavior profiles

Create original SZL profiles for high-autonomy operator, adversarial verifier, evidence-first researcher, bounded coding agent, database change controller, and incident commander. Profiles may alter disposition, grounding, tool discipline, verification, and voice; they must never claim to raise weight-bound capability. Store body digests, sources, rights, and receipts. Do not include leaked/verbatim third-party prompts.

### Evaluation and promotion

Evaluate per axis: calibration, instruction compliance, refusal precision, tool safety, code correctness, retrieval grounding, long-context retention, adversarial robustness, source attribution, license-boundary reasoning, execution-boundary reasoning, latency, memory, and measured energy/cost. Publish raw cases, harness version, preregistration, seeds, and hardware identity. Never train on holdouts or reduce the decision to one vanity score.

Preserve failed candidates and receipts; create new candidate IDs; freeze holdouts before training; require all mandatory holdouts; prohibit automatic retry and threshold weakening; separate qualification from promotion; emit signed train/merge/quantize/evaluate/publish/deploy receipts.

## W5 — SZL Forge productionization

**Scope:** `szl-forge`.

- Reconcile Forge, Khipu, ReceiptAgent, Nemo, GGUF, Ollama, Unsloth, evaluation, signing, and publication paths.
- Replace laptop assumptions with explicit hardware profiles and bounded resource checks.
- Isolate failures between training, merge, conversion, quantization, import, inference, and evaluation.
- Make commands rerun-safe and content-addressed.
- Bind cards/artifacts to exact source, dataset, config, toolchain, and weight digests.
- Integrate admitted models with A11oy registry/routing without fabricated availability.

## W6 — Series A database upgrade

**Scope:** `platform` and the managed production database.

- Recapture provider, PostgreSQL version, schema, trackers, journal, manual migrations, and deployed application revision.
- Make journal-based migration the default. Restrict `drizzle-kit push` to explicitly acknowledged local development and block `push --force` from production/CI.
- Validate every journal file/hash, tracker history, manual checksum, unknown tracker entry, truly empty fresh target, and schema-equivalent legacy baseline. Never backfill trackers from a single sentinel table.
- Require exact protected-main OID, immutable migration manifest, encrypted backup, successful restore to a distinct empty target, schema/sequence/constraint/index/extension/row-count comparison, approved change ticket, advisory lock, and write-fence evidence for destructive or rewrite-risk plans.
- Apply only through managed database secrets.
- Verify API, workers, telemetry, trackers, schema fingerprint, and representative governed workflows.
- Emit a signed before/after cutover receipt. Do not claim completion without live database evidence.

## W7 — MCP authority unification

**Scope:** `hatun-mcp`, `platform`, `a11oy`.

- Inventory standalone Hatun MCP and the monorepo gateway.
- Select one canonical source contract based on doctrine, consumers, deployment ownership, tests, and release path.
- Generate compatibility layers from the canonical schema.
- Remove duplicated hand-maintained definitions.
- Add cross-repository drift tests for method names, input/output schemas, policy gates, receipts, and errors.
- Publish compatibility/migration notices before retirement.
- Do not create a third MCP registry.

## W8 — Responsive investor/developer presentation

Across GitHub, Hugging Face, domains, and deployable applications:

- Verify mobile, tablet, laptop, and widescreen breakpoints.
- Eliminate clipping, overflow, illegible tables, tiny touch targets, dead links, placeholder states, and inconsistent navigation.
- Use original SZL visual language, not copied vendor trade dress.
- Present one product hierarchy: flagships, organs, research, infrastructure, retired history.
- Provide investor-facing value/moat/governance/evidence/risk and developer-facing architecture/quick-start/API/model/data/deployment/test/verification guidance.
- Capture live screenshots for every modified public surface.
- Run accessibility, route, link, browser, performance, and mobile viewport gates.

## W9 — Protected deployments and final proof

For each shipping surface:

1. Deploy only after protected merge.
2. Bind deployment to merged full OID and immutable artifact digest.
3. Independently verify build-info, health, critical routes, browser state, model registry, receipts, and public claims.
4. Require rollback or forward-repair instructions and evidence.
5. Attach deployment receipts to PRs and issues.

## Repository mutation protocol

For every mutation:

1. Fetch protected default branch and capture full base OID.
2. Establish clean baseline and record relevant tests.
3. Create one non-protected branch for one coherent concern.
4. Implement the smallest root-cause change.
5. Run native tests, security gates, source-contract checks, and secret scans.
6. Create a DCO-certified commit under repository signing policy.
7. Push normally and open a PR documenting root cause, scope, tests, evidence, deployment boundary, and repair plan.
8. Observe required checks to terminal state, resolve conversations, and revalidate exact head.
9. Use the protected merge path.
10. Deploy merged exact revision and perform independent live readback.
11. Attach receipts and close governing issues only after acceptance passes.

## Required final artifacts

Maintain under this coordination directory:

- `current-state.json`
- `workgraph.json`
- `decision-log.md`
- `risk-register.md`
- `claim-drift-report.md`
- `github-estate-manifest.json`
- `hf-estate-manifest.json`
- `model-admission-report.json`
- `database-cutover-receipt.json` or a managed-prerequisite block receipt
- `deployment-reconciliation.json`
- `final-state.json`
- `FINAL_SERIES_A_REPORT.md`
- `receipts/`

## Definition of done

Complete only when current manifests cover every in-scope repository/Hub asset; required PRs are protected-merged or terminally failed; checks are terminal/explained; no public UI is indefinitely transient; Killinchu status is capability-axis accurate; source/artifact/publication/deployment revisions agree; promoted artifacts have rights/provenance/digests/evaluations/receipts; the database is live-verified or explicitly blocked; one MCP authority exists; responsive presentation is verified; stale claims are removed; the final receipt verifies offline; and residual risks are explicit.

Do not say “complete,” “operational,” “production,” “Series A ready,” or “verified” without matching evidence.

## Mandatory final response

Return one row per workstream with repository/organization, base OID, branch/PR, merged OID, deployed OID or digest, tests, receipt, terminal status, and residual risk.

Allowed terminal statuses:

- `VERIFIED_CURRENT`
- `TERMINAL_FAILURE`
- `BLOCKED_MANAGED_PREREQUISITE`

Do not end with a plan. End with the evidence-backed execution matrix.

## Machine-readable workgraph

```json
{
  "schema": "szl.codex.whole-thread-workgraph/v1",
  "generated_at": "2026-08-11T00:00:00Z",
  "tasks": [
    {"id":"W0","name":"Immutable estate recapture","depends_on":[],"mutates":false},
    {"id":"W1","name":"GitHub organization convergence","depends_on":["W0"],"mutates":true},
    {"id":"W2","name":"A11oy public truth and terminal health","depends_on":["W0"],"mutates":true},
    {"id":"W3","name":"Hugging Face estate convergence","depends_on":["W0","W1"],"mutates":true},
    {"id":"W4","name":"Governed model frontier","depends_on":["W0"],"mutates":true},
    {"id":"W5","name":"SZL Forge productionization","depends_on":["W0","W4"],"mutates":true},
    {"id":"W6","name":"Series A database upgrade","depends_on":["W0"],"mutates":true,"managed_prerequisites":["DATABASE_URL","backup target","change approval","signing identity"]},
    {"id":"W7","name":"MCP authority unification","depends_on":["W0"],"mutates":true},
    {"id":"W8","name":"Responsive investor/developer presentation","depends_on":["W1","W2","W3"],"mutates":true},
    {"id":"W9","name":"Protected deployments and final proof","depends_on":["W2","W3","W5","W6","W7","W8"],"mutates":true}
  ],
  "allowed_terminal_statuses":["VERIFIED_CURRENT","TERMINAL_FAILURE","BLOCKED_MANAGED_PREREQUISITE"]
}
```
