name: Azure Web Apps Node.js (Disabled)

  on:
    workflow_dispatch:

  jobs:
    placeholder:
      runs-on: ubuntu-latest
      steps:
        - run: echo "This workflow has been disabled. Use szl-holdings-platform repo instead."
  